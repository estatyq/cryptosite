# 🎨 Гайд з кастомізації MF PRIME CLUB

Цей документ показує, як змінити вигляд та контент вашого лендінгу під ваш бренд.

## 📝 КОНТЕНТ

### Заголовок Hero

Знайдіть в `index.html` рядок ~110:
```html
<h1 class="text-hero metallic-gradient mb-8 leading-tight">
    MF<br>PRIME<br>CLUB
</h1>
```

Змініть на:
```html
<h1 class="text-hero metallic-gradient mb-8 leading-tight">
    ВАШЕ<br>ІМЯ<br>КЛУБУ
</h1>
```

### Підзаголовок Hero

Рядок ~114:
```html
<p class="text-subhero text-gray-400 mb-12 leading-relaxed max-w-md">
    Доступ назавжди. Закритий криптоклуб і ком'юніті трейдерів.
</p>
```

Замініть на:
```html
<p class="text-subhero text-gray-400 mb-12 leading-relaxed max-w-md">
    ВАШ ПІДЗАГОЛОВОК. ОПИС КЛУБУ.
</p>
```

### Девіз

Рядок ~122:
```html
<p class="text-xs text-gray-500 tracking-widest">
    no risk — no porsche
</p>
```

Змініть на:
```html
<p class="text-xs text-gray-500 tracking-widest">
    ВАШ_ДЕВІЗ_ТУТ
</p>
```

### Розділ Переваг

Рядок ~193:
```html
<h2 class="text-4xl md:text-5xl font-800 mb-4">
    MF PRIME CLUB
</h2>
```

Рядок ~197:
```html
<p class="text-xl text-gray-400 font-light">
    Закритий канал
</p>
```

### Список Переваг

Кожна картка має цю структуру (приклад рядків ~219-226):

```html
<div class="card-premium p-6 rounded-2xl scroll-reveal icon-item">
    <div class="text-3xl mb-4">📊</div>
    <h3 class="text-xl font-700 mb-2">Мої особисті угоди</h3>
    <p class="text-gray-400">коли заходжу, де ставлю стоп/тейк, що роблю далі</p>
</div>
```

Змініть:
- `📊` → ваша іконка (emoji)
- `Мої особисті угоди` → заголовок
- `коли заходжу...` → опис

### Секція Оплати

Рядок ~307:
```html
<p class="text-6xl font-800 bg-gradient-to-r from-yellow-400 to-yellow-500 bg-clip-text text-transparent mb-4">
    249 USDT
</p>
```

Замініть `249` на вашу ціну.

### Адреса Гаманця

**ВАЖНО:** ЗМІНІТЬ В ОБОХ МІСЦЯХ!

Рядок ~324:
```html
<span class="text-yellow-400">TKB8LTG1HRpySZ3w6jjaGsvuqdZRt48hiv</span>
```

Рядок ~431:
```javascript
const address = 'TKB8LTG1HRpySZ3w6jjaGsvuqdZRt48hiv';
```

## 🎨 КОЛЬОРИ

### Золота кнопка

Знайдіть в `<style>` (рядки ~74-83):

```css
.btn-gold {
    background: linear-gradient(135deg, #FFD700 0%, #FFC700 100%);
    color: #0B0B0B;
    ...
}

.btn-gold:hover {
    background: linear-gradient(135deg, #FFE700 0%, #FFD700 100%);
    ...
}
```

Замініть `#FFD700` на ваш колір:

```css
.btn-gold {
    background: linear-gradient(135deg, #ВАШ_КОЛІР1 0%, #ВАШ_КОЛІР2 100%);
    ...
}
```

### Металевий градієнт (текст)

Рядки ~108-114:
```css
.metallic-gradient {
    background: linear-gradient(135deg, #E8E8E8 0%, #BFBFBF 50%, #909090 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}
```

Замініть кольори:
```css
background: linear-gradient(135deg, #ВАША_ТІНЬ1 0%, #ВАША_ТІНЬ2 50%, #ВАША_ТІНЬ3 100%);
```

### Фоновий колір

Рядок ~19:
```css
body {
    background: radial-gradient(circle at 30% 40%, #1A1A1A 0%, #0B0B0B 80%);
}
```

Замініть:
```css
background: radial-gradient(circle at 30% 40%, #ВАША_ТІНЬ1 0%, #ВАША_ТІНЬ2 80%);
```

### Карток-премиум

Рядки ~85-91:
```css
.card-premium {
    background: rgba(20, 20, 20, 0.6);
    border: 1px solid rgba(232, 232, 232, 0.1);
}
```

- `rgba(20, 20, 20, 0.6)` = темний фон картки (red, green, blue, opacity)
- `rgba(232, 232, 232, 0.1)` = колір бордюру (білий, 10% opacity)

### Дим/Smoke ефект

Рядки ~36-50:
```css
.smoke-bg {
    background: 
        radial-gradient(ellipse 800px 600px at 20% 30%, rgba(255, 255, 255, 0.08) 0%, transparent 50%),
        radial-gradient(ellipse 1000px 800px at 80% 70%, rgba(0, 0, 0, 0.3) 0%, transparent 60%),
        radial-gradient(ellipse 600px 600px at 50% 50%, rgba(128, 128, 128, 0.05) 0%, transparent 70%),
        linear-gradient(180deg, rgba(255,255,255,0.02) 0%, transparent 50%, rgba(0,0,0,0.1) 100%);
}
```

Змініть `rgba` значення:
- Перше число = Red (0-255)
- Друге число = Green (0-255)
- Третє число = Blue (0-255)
- Четверте число = Opacity (0-1, де 0 = прозорий, 1 = непрозорий)

## 🔤 ШРИФТИ

### Змінити шрифт

Рядки 7-8:
```html
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&family=Inter+Tight:wght@400;500;600;700&display=swap" rel="stylesheet">
```

Замініть на інші Google Fonts:

```html
<link href="https://fonts.googleapis.com/css2?family=ВАША_СІМ'Я_1:wght@400;500;600;700;800&family=ВАША_СІМ'Я_2:wght@400;500;600;700&display=swap" rel="stylesheet">
```

Потім оновіть CSS (рядок 10):
```css
* {
    font-family: 'ВАША_СІМ'Я_1', 'ВАША_СІМ'Я_2', sans-serif;
}
```

### Вага шрифту

- `font-400` = Звичайний
- `font-500` = Середній
- `font-600` = Напів-жирний (semibold)
- `font-700` = Жирний (bold)
- `font-800` = Дуже жирний (extra bold)

Замініть в класах Tailwind, наприклад:
```html
<h1 class="font-800">...</h1>  <!-- Дуже жирний -->
<h1 class="font-600">...</h1>  <!-- Напів-жирний -->
```

## 🖼️ ЛОГОТИП

### Замінити логотип в Footer

Рядок ~379:
```html
<div class="text-4xl mb-4">🏆</div>
```

Варіант 1: Іконка
```html
<div class="text-4xl mb-4">🎯</div>  <!-- або інша іконка -->
```

Варіант 2: SVG
```html
<div class="mb-4">
    <svg width="60" height="60" viewBox="0 0 60 60" fill="none">
        <!-- ВАШ SVG КОД ТУТЖ -->
    </svg>
</div>
```

Варіант 3: Зображення
```html
<img src="logo.png" alt="Logo" class="w-16 h-16 mb-4">
```

## 📱 АДАПТИВНІСТЬ

Більшість адаптивності уже налаштовано, але якщо хочете змінити:

### Зміна точки розірвання (breakpoint)

Рядок ~180:
```css
@media (max-width: 768px) {
    /* Стилі для мобілей */
}
```

- `768px` = iPad / планшет
- Змініть на `640px` для меньшего экрана
- Змініть на `1024px` для більшого

### Гранде сітка

Рядок ~270:
```html
<div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12">
```

- `grid-cols-1` = 1 колона на мобілі
- `md:grid-cols-2` = 2 колони на планшеті (768px+)

Змініть:
- `md:grid-cols-3` = 3 колони
- `lg:grid-cols-4` = 4 колони на великих екранах

## 🎬 АНІМАЦІЇ

### Вимкнути анімації

Комментуйте рядки ~116-135 або замініть на:

```css
.fade-in {
    animation: none;
    opacity: 1;
}
```

### Змінити швидкість анімацій

Рядки ~116-120:
```css
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.fade-in {
    animation: fadeIn 0.8s ease-out forwards;  /* Змініть 0.8s */
}
```

- `0.3s` = швидко
- `0.8s` = нормально
- `2s` = повільно

### Змінити ефект анімацій

`ease-out` → `ease-in` / `ease-in-out` / `linear`

## 🌐 МОВА

### Замінити на іншу мову

Просто замініть весь текст! Структура залишається такою ж.

Головне - не міняйте HTML теги та класи, тільки текст всередину них.

## 🔧 ДОДАТКОВІ НАЛАШТУВАННЯ

### Вісь контенту (max-width)

Рядок ~227:
```html
<div class="max-w-6xl mx-auto">
```

- `max-w-4xl` = вужче
- `max-w-6xl` = нормально
- `max-w-7xl` = ширше

### Padding (відступи)

Приклад рядка ~268:
```html
<div class="card-premium p-6 rounded-2xl">
```

- `p-4` = малий
- `p-6` = нормальний
- `p-8` = великий
- `p-12` = дуже великий

### Border radius (закруглення)

Рядок ~268:
```html
<div class="... rounded-2xl">
```

- `rounded` = трохи закруглено
- `rounded-lg` = закруглено
- `rounded-2xl` = дуже закруглено
- `rounded-full` = цілком круглое

## 💡 ПОРАДИ

1. **Не міняйте JavaScript** - він контролює функціональність
2. **Резервне копіювання** - збережіть копію перед змінами
3. **Тестування** - завжди перевіряйте на мобілі та десктопі
4. **Google Fonts** - пошук на [fonts.google.com](https://fonts.google.com)
5. **Кольори** - використовуйте [coolors.co](https://coolors.co) для гармонійної палітри

## 🎨 ПРИКЛАДИ ПАЛІТР

### Темно-синя (Modern Tech)
```css
#0D1B2A (фон)
#1B263B (картки)
#2E8B96 (акцент)
#E0E1DD (текст)
```

### Неонова (Cyberpunk)
```css
#0a0e27 (фон)
#1a1a2e (картки)
#ff006e (акцент)
#f0f3ff (текст)
```

### Природна (Organic)
```css
#1a1a1a (фон)
#2d2d2d (картки)
#3d9970 (акцент)
#e8e8e8 (текст)
```

---

**Потребна допомога?** Дивіться README.md або DEPLOYMENT.md

