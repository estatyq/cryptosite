# 🚀 Гайд по розгортанню MF PRIME CLUB

## 📋 Вимоги

- Сучасний браузер (Chrome, Firefox, Safari, Edge)
- Будь-який веб-сервер або просто файловий доступ

## 🏃 Швидкий старт

### 1️⃣ Локально (найпростіше)

**Варіант A: Просто відкрийте файл**
```bash
# Windows
start index.html

# macOS
open index.html

# Linux
xdg-open index.html
```

**Варіант B: Локальний веб-сервер**

Якщо у вас є Python:
```bash
# Python 3.x
python -m http.server 8000

# Python 2.x (старше)
python -m SimpleHTTPServer 8000
```

Якщо у вас є Node.js:
```bash
npx http-server
```

Якщо у вас є PHP:
```bash
php -S localhost:8000
```

Потім відкрийте браузер на `http://localhost:8000`

### 2️⃣ VSCode + Live Server (рекомендується)

1. Встановіть розширення **Live Server** від Ritwick Dey
2. Клікніть правою кнопкою на `index.html`
3. Виберіть "Open with Live Server"
4. Браузер автоматично відкриється

### 3️⃣ Netlify (безплатний хостинг)

1. Завантажте проект на GitHub
2. Перейдіть на [netlify.com](https://netlify.com)
3. Натисніть "New site from Git"
4. Виберіть ваш репозиторій
5. Налаштування:
   - Build command: (залиште пусто)
   - Publish directory: .
6. Натисніть "Deploy site"

### 4️⃣ Vercel (速度максимальна)

1. Завантажте на GitHub
2. Перейдіть на [vercel.com](https://vercel.com)
3. Натисніть "Import Project"
4. Виберіть GitHub репозиторій
5. Натисніть "Deploy"

### 5️⃣ Традиційний хостинг (cPanel, etc.)

1. Завантажте `index.html` та `README.md` через FTP
2. Переконайтеся, що файли в кореневій папці або у папці public_html
3. Відкрийте ваш домен у браузері

## ⚙️ Налаштування для вашого проекту

### Зміна адреси гаманця

Знайдіть в `index.html` (рядок ~324):
```html
<span class="text-yellow-400">TKB8LTG1HRpySZ3w6jjaGsvuqdZRt48hiv</span>
```

Замініть на вашу адресу:
```html
<span class="text-yellow-400">YOUR_WALLET_ADDRESS_HERE</span>
```

**ДЛЯ СКРИПТУ** (рядок ~375):
```javascript
const address = 'TKB8LTG1HRpySZ3w6jjaGsvuqdZRt48hiv';
```

Замініть обидва місця на одну адресу!

### Зміна ціни

В секції оплати (рядок ~307):
```html
<p class="text-6xl font-800 bg-gradient-to-r from-yellow-400 to-yellow-500 bg-clip-text text-transparent mb-4">
    249 USDT
</p>
```

Замініть `249` на вашу ціну.

### Зміна таблиці цін

Рядки 313-318:
```html
<div class="price-item bg-slate-800/50 border border-gray-700 rounded-lg px-6 py-3 text-gray-300">249</div>
<div class="price-item bg-slate-800/50 border border-gray-700 rounded-lg px-6 py-3 text-gray-300">457</div>
<!-- ... та ін. -->
```

Замініть числа на ваші варіанти цін.

### Зміна контенту

Просто редагуйте текст у відповідних секціях:

- **Заголовок**: `<h1>MF PRIME CLUB</h1>` (рядок ~110)
- **Підзаголовок**: `<p class="text-subhero...">Доступ назавжди...</p>` (рядок ~114)
- **Девіз**: `<p class="text-xs...">no risk — no porsche</p>` (рядок ~122)

### Зміна кольорів

Редагуйте CSS в `<style>` (рядок 20-140):

```css
/* Темний фон */
background: radial-gradient(circle at 30% 40%, #1A1A1A 0%, #0B0B0B 80%);

/* Золота кнопка */
.btn-gold {
    background: linear-gradient(135deg, #FFD700 0%, #FFC700 100%);
}

/* Текст */
color: #FFFFFF;
```

## 🔒 Безпека

### ВАЖНО: Адреса гаманця видна в JavaScript!

Ваша адреса видна у вихідному коді сторінки. Це **НОРМАЛЬНО**, оскільки:
- Публічна адреса гаманця не є таємною
- Будь-хто може передати гроші на публічну адресу
- Це аналогічно опублікуванню номера рахунку для платежів

**Але НІКОЛИ не вмішуйте:**
- Приватні ключі
- Seed phrases
- API keys

## 📱 Перевірка адаптивності

1. **Відкрийте сторінку в браузері**
2. **Натисніть F12** (DevTools)
3. **Натисніть Ctrl+Shift+M** (Toggle Device Toolbar)
4. Перевірте на різних розмірах:
   - iPhone 12: 390x844
   - iPad: 768x1024
   - Desktop: 1920x1080

## 🐛  Troubleshooting

### Проблема: Шрифти не завантажуються

**Рішення:**
- Перевірте інтернет-з'єднання
- Видаліть кеш браузера (Ctrl+Shift+Delete)
- Спробуйте інший браузер

### Проблема: Копіювання не працює

**Рішення:**
- Перевірте, чи це HTTPS (деякі браузери блокують Clipboard API на HTTP)
- Спробуйте інший браузер
- Переконайтеся, що JavaScript увімкнено

### Проблема: Анімації не працюють

**Рішення:**
- Перевірте сумісність браузера
- Видаліть кеш (Ctrl+Shift+Delete)
- Перевантажте сторінку (F5)

### Проблема: Макет схожений на "неправильний"

**Рішення:**
- Очистіть кеш браузера
- Перевірте, чи екран досить широкий (мінімум 320px)
- Спробуйте інший браузер

## 📊 Analytics (опціонально)

Якщо хочете отримувати статистику, додайте Google Analytics:

Добавьте перед `</head>`:
```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-YOUR_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-YOUR_ID');
</script>
```

Замініть `G-YOUR_ID` на ваш ID з Google Analytics.

## 🎯 Performance Tips

1. **Компресія файлів** - хостинг автоматично gzip'ує HTML
2. **CDN** - Netlify і Vercel автоматично розповсюджують файли
3. **Кеш** - встановіть headers кешування на хостингу
4. **Image optimization** - у нас немає растрових зображень, тільки CSS
5. **Lazy loading** - уже реалізовано через Intersection Observer

## 💬 Підтримка користувачів

Додайте на сторінку контактну інформацію для підтримки. Редагуйте footer (рядок 343):

```html
<p class="text-gray-400 text-sm">
    © MF Prime Club 2025<br>
    <span class="text-gray-500">Premium Community</span><br>
    <span class="text-yellow-500 text-xs">Підтримка: support@example.com</span>
</p>
```

## 🔄 Оновлення сторінки

Щоб оновити сторінку:
1. Редагуйте файл `index.html`
2. Зберегти (Ctrl+S)
3. На сервері:
   - Vercel/Netlify: Автоматично розгортає при push на GitHub
   - FTP: Перезавантажте файл
   - cPanel: Обновите файл через File Manager

## 📞 Додаткова допомога

- **HTML питання**: [MDN Web Docs](https://developer.mozilla.org/)
- **Tailwind CSS**: [Tailwind Docs](https://tailwindcss.com/docs)
- **JavaScript**: [JS.info](https://javascript.info/)
- **Хостинг питання**: Звертайтеся до вашого хостера

---

**Остання оновлена**: November 2025

